var path = require('path');

module.exports = {
    'config': path.resolve('./', 'config/config.json'), // Set the config file path to src/config/config.json
    'migrations-path': path.resolve('./src', 'migrations'), // Set the migrations path to the src/migrations folder
    'models-path': path.resolve('./src', 'models'), // Set the models path to the src/models folder
    'seeders-path': path.resolve('./src', 'seeders'), // Set the seeders path to the src/seeders folder
};